# Pages and Features

This is the non-technical page catalog for the current application. Each row answers the same seven questions: purpose, access, displayed information, actions, result, automatic behavior, and conditions/restrictions. Routes are shown so operators and testers can identify the exact screen. A page marked **unwired** exists as a source file but has no current route and is not part of normal operation.

## Public and sign-in pages

| Page and route | 1. Purpose | 2. Access | 3. Information displayed | 4. User actions | 5. Result | 6. Automatic behavior | 7. Conditions and restrictions |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Student/Parent Login — `/` (`Auth/StudentParentLogin.vue`) | Public entry and school information page. | Visitors; authenticated users are redirected by role. | Login form, saved local profiles, Parent option when enabled, information/about/contact sections. | Choose a saved profile, enter credentials, remember the account, open password recovery, or visit Panel/Staff login. | Student or permitted Parent reaches the portal. | Saved-profile preference is processed after login; sessions regenerate. | Only Student/Parent accounts; Parent option/login disappears and is rejected when Parent Portal is off. |
| Staff Login — configured private route (`Auth/StaffLogin.vue`) | Sign in Admin, Instructor, Registrar, or Clinic staff. | Visitors and already signed-in staff. | Staff form, saved profiles, password recovery, links back to public/Panel login. | Sign in, remember profile, remove saved profile, recover password. | Redirects to the role dashboard; Instructor goes to verification. | Login throttling and session regeneration apply. | Route comes from `SECURE_LOGIN_ROUTE`; other roles are rejected. Never publish the production value. |
| Attendance Panel Login — `/attendance-control-panel/login` | Connect a Console account to a room/device. | Visitors and Console operators. | Active laboratories, panel/device state, room and PIN steps. | Select room, enter PIN, sign in/open panel, return home. | Opens the room panel session. | Uses assigned active device PIN or global fallback; records/open session state. | Disabled device or wrong PIN blocks access; panel routes require Console role. |
| Forgot Password — `/forgot-password` | Request a password-reset email. | Visitors. | Email form, status, context-aware back link. | Submit email. | Shows a neutral success status and sends mail when eligible. | Fortify creates a time-limited token; repeated requests are throttled. | Console accounts are deliberately not reset by email; mail must be configured. |
| Reset Password — `/reset-password/{token}` | Replace a forgotten password. | Holder of a valid reset link. | Email/token context and new-password fields. | Submit matching password and confirmation. | Password changes and normal post-reset redirect occurs. | Token is consumed; response redirects by account role. | Token expires after configured period; password rules depend on environment. |
| First Login Password — `/first-login/password` | Replace a temporary account password. | Authenticated non-Console account with `must_change_password`. | New password and confirmation. | Save password. | Flag is cleared and the user can continue. | Middleware redirects every other page here until complete. | Throttled; Console is exempt. |
| Instructor Verification — `/instructor/verify` | Extra identity check after Instructor login. | Authenticated Instructor. | Face availability, OTP controls, saved security questions/setup. | Verify face, send/enter OTP, set or answer security questions, log out. | Successful verification opens Instructor dashboard for the session. | OTP expires after 10 minutes; each staff login clears prior verification. | Face needs enrolled image/provider; OTP needs valid mail; answers are hashed and normalized. |
| Two-Factor Challenge — `/two-factor-challenge` | Complete two-factor sign-in. | Account currently in Fortify challenge flow. | Authenticator-code/recovery-code form. | Submit a valid code. | Completes sign-in. | Fortify rate-limits challenges. | Only appears for accounts with confirmed two-factor authentication. |
| Confirm Password — `/user/confirm-password` | Reconfirm identity before a sensitive action. | Signed-in user sent by Fortify. | Password field. | Confirm password. | Opens the protected action for the configured confirmation window. | Confirmation state is stored in session. | Used by sensitive Fortify operations such as two-factor settings. |
| Register — `/register` (`Auth/Register.vue`) | General registration UI retained from the starter application. | Visitor. | Registration form. | Submit name/email/password. | Custom controller creates a default-role user and redirects. | Password is hashed. | Public registration is enabled in routes; production deployments should decide whether it should remain available. |
| About — `/about` (`About.vue`) | Minimal placeholder About screen. | Anyone. | A single About heading. | None. | No state change. | None. | This is separate from the richer about content embedded in the public login. |
| Health Check — `/up` | Let monitoring confirm the Laravel app responds. | Anyone/network monitor. | Machine-oriented health response. | Send request. | Healthy/unhealthy response. | Laravel generates the result. | Does not prove database, mail, SMS, scheduler, or face provider health. |

## Shared signed-in pages

| Page and route | 1. Purpose | 2. Access | 3. Information displayed | 4. User actions | 5. Result | 6. Automatic behavior | 7. Conditions and restrictions |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Messenger — `/messages` (`Messages/Index.vue`) | Private conversations across non-Console roles. | Admin, Instructor, Clinic, Registrar, Student, Parent; Parent only when portal enabled. | Recipient search, conversations, unread state, text and attachment previews. | Select recipient, send text/file, open/download attachment, mark read; staff may forward an eligible excuse PDF to a linked Parent. | Creates encrypted conversation record and optional attachment. | Layout polls unread state every 10 seconds; email alerts use a cooldown. | Only sender/recipient can access attachment; allowed file limits apply; Console excluded. |
| New Message — `/messages/new` (`Messages/Create.vue`) | Legacy public/student-to-Instructor message form. | Route is currently public. | Instructor selector and sender/message/attachment form. | Send to an Instructor. | Creates a legacy `messages` inbox record. | Activity can be logged. | This route lacks the authenticated group used by Messenger; treat as a legacy/public contact path and review before production use. |
| Shared Reports — `/reports` (`Reports/Index.vue`) | Role-specific summaries, charts, filters, and CSV export. | Admin, Instructor, Clinic, Registrar, Student, Parent. | Role-scoped cards/charts, date/year filters, detail rows. | Filter, select permitted academic year, download CSV. | Re-runs scoped queries or streams CSV. | Defaults to current/latest year; export uses visible filters and is audited. | Instructor, Student, and Parent scopes are server-enforced. Some current queries use enrollment status `active` although normal enrollment creation uses `enrolled`; verify report counts. |
| Profile Settings — `/settings/profile` | Edit the signed-in account profile. | Authenticated user. | Name/email/profile fields. | Update profile; verified users may delete account. | User row changes or is soft-deleted and session is invalidated on deletion. | Mutations are audited. | Validation and current-password rules apply; page uses starter-kit settings wrapper. |
| Password Settings — `/settings/password` | Change the current password. | Authenticated and email-verified user. | Current/new password form. | Update password. | Password hash changes. | Request is throttled and audited. | Console reset is separately restricted; current password must match. |
| Appearance Settings — `/settings/appearance` | Change light/dark/system appearance. | Authenticated and verified user. | Theme choices. | Select appearance. | Browser appearance preference changes. | Stored client-side/cookie, not in an application table. | No school data changes. |
| Two-Factor Settings — `/settings/two-factor` | Enable, confirm, disable, or recover two-factor authentication. | Authenticated, verified user after password confirmation. | QR/secret state and recovery codes. | Configure or disable two factor; regenerate/view codes through Fortify controls. | Encrypted two-factor fields on account change. | Fortify manages confirmation and recovery data. | Requires recent password confirmation and framework-provided endpoints. |

## Admin and Instructor workspace

| Page and route | 1. Purpose | 2. Access | 3. Information displayed | 4. User actions | 5. Result | 6. Automatic behavior | 7. Conditions and restrictions |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Dashboard — `/admin/dashboard` (`Auth/Admin/Dashboard.vue`) | Operational overview for Admin or Instructor. | Admin; verified Instructor. | Counts, schedules, current academic context, attendance summaries, recent activity/messages. | Change year/semester filters and open related pages. | Navigates or refreshes scoped dashboard data. | Instructor data is automatically limited to linked Instructor profile. | Instructor without profile/verification cannot see normal scoped data. |
| Academic Years — `/admin/academic-years` | Manage year/semester lifecycle and rollover. | Admin only. | Draft/active/closed/archived years, active semester, audits, rollover previews/history, legacy-fallback counters. | Create/edit, activate, close, archive, reopen with reason, preview and execute semester/full rollover. | Changes lifecycle or creates mapped enrollments/sections and audit rows. | Activating closes old active year; rollover is transactional and repeat-safe. | Closed/archived years are read-only; full rollover needs a different draft destination; semester rollover is First-to-Second only. |
| Strands — `/admin/strands` | Maintain strands/tracks. | Admin only. | Code, name, department, status, counts/filters. | Search, add, edit, delete. | Master record changes. | Related lists update on next request and operation is audited. | Unique code; referenced records or soft-delete behavior can prevent/affect removal. |
| Sections — `/admin/sections` | Maintain year- and semester-specific sections. | Admin only. | Academic-year/semester filters, strand, grade, status and reference state. | Filter, add, edit, delete. | Section and academic context change. | Current year/semester defaults are applied. | Only draft/active current context is writable; referenced or historical sections cannot be safely removed. |
| Subjects — `/admin/subjects` | Maintain reusable subject catalog and yearly offerings. | Admin only. | Subject code/name/description/unit plus offerings by year, semester, section, instructor. | Add/edit/delete subject; add/delete offering; remove offering instructor. | Catalog/offering records change. | Offering inherits year from section. | Historical/closed offering is read-only; referenced subject/offering may be protected. |
| Schedules — `/admin/schedules` | Build class time/room assignments. | Admin manages; verified Instructor views own schedules. | Year/semester, room/lab, offering, instructor, section, weekday and times. | Admin filters/adds/edits/deletes; Instructor filters/views. | Schedule changes or view refreshes. | Offering supplies subject, section, year, semester, and Instructor. | Only current year/semester is writable; history blocks deletion; overlapping schedules are not detected. |
| Laboratories & Devices — `/admin/active-devices` | Manage rooms, panel devices, access PINs, and live sessions. | Admin only. | Laboratories, assigned devices, enabled state, panel sessions, feature switches/fallback settings. | Add/edit/delete lab/device, assign device, enable/disable, change global/device PIN, force logout, open panel login. | Configuration changes; force logout ends panel/session state. | Latest room session determines live status. | Active device session must be logged out before device deletion; device PIN overrides global fallback. |
| Laboratories (legacy focused page) — `/admin/laboratories` | CRUD view dedicated to laboratories. | Admin only. | Laboratory name, description, location, status. | Add/edit/delete. | Laboratory row changes. | Audited. | Device management belongs on Laboratories & Devices; references may block deletion. |
| Instructor Management — `/admin/instructors` | Maintain Instructor account/profile links. | Admin only. | Instructor number, user, strand, status, RFID. | Search, add, edit, delete. | Creates/updates both user and Instructor profile as required. | Temporary password/first-login and audit rules apply. | Unique email, number, RFID; deleting linked history can be restricted or cascade according to schema. |
| Student Management — `/admin/students` | Maintain permanent student identity, yearly enrollment, portal account, and Parent links. | Admin manages; verified Instructor gets a read-only assigned view. | Student identity/status, current placement, enrollment history, Parent accounts, RFID and face indicators. | Admin filters/adds/edits/deletes, resets student password, creates/links/edits/unlinks Parent; UI also contains face upload/delete controls. | Student/enrollment/account/link changes and temporary-password message. | Student portal account is created/updated; legacy placement mirrors normalized enrollment. | New/changed placement must match current year/semester/section. Face controls currently name routes that are not registered, so use Registrar enrollment until code is corrected. |
| User Management — `/admin/users` | Manage privileged operational accounts. | Admin; Root Admin for Admin-account changes. | Admin/Registrar/Clinic accounts, root flag, status and action permissions. | Create/edit/delete eligible users and set Root Admin flag. | Account created with temporary password, changed, or soft-deleted. | First-login password replacement and audit apply. | Cannot delete self or last Root Admin; standard Admin cannot manage Admin accounts. |
| Attendance Subject Selection — `/admin/attendance/logs` (`Attendance/SubjectSelection.vue`) | Entry to attendance analytics. | Admin; verified Instructor. | Subjects, totals, filters, year/semester. | Filter and open a subject. | Opens subject dashboard; one-subject Instructor may be redirected automatically. | Status totals include physical and online sessions. | Instructor only sees assigned subjects. |
| Attendance Subject Dashboard — `/admin/attendance/subjects/{subject}` | Review one subject's sessions and totals. | Admin; assigned verified Instructor. | Subject metadata, overview totals, physical and online session cards. | Open summary or session. | Navigates to detail. | Stable subject color derives from subject identity. | Server verifies Instructor assignment. |
| Attendance Summary — `/admin/attendance/subjects/{subject}/summary` | Full roster status summary across sessions. | Admin; assigned verified Instructor. | Student rows and status totals. | Open student history; export CSV/XLSX. | Detail view or download. | Missing/ended-session outcomes are derived. | Scope and selected academic context apply. |
| Attendance Student History — `/admin/attendance/subjects/{subject}/students/{student}` | Show one student's physical and online attendance history in the subject. | Admin; assigned verified Instructor. | Session dates, class type, status, times, evidence/tap context. | Return/open relevant record. | No direct data change. | Physical and online rows are combined. | Student must belong to accessible subject scope. |
| Attendance Session Details — `/admin/attendance/subjects/{subject}/sessions/{session}` | Inspect/edit one physical or online session roster. | Admin; assigned verified Instructor. | Roster, Present/Late/Pending/Absent/Excused, session metadata. | Export; eligible Instructor changes status and adds Excused reason. | Status and audit record change. | Ended missing students derive as Absent; edits create audit data. | Only assigned Instructor can edit, within configured day window; Admin view is supervisory. |
| Legacy Attendance Logs — `/admin/attendance/logs/legacy` (`AttendanceLogs.vue`) | Older tap/log table retained for compatibility and manual corrections. | Admin; verified Instructor. | Combined tap events, filters, evidence, generated No Tap rows. | Filter, inspect evidence, eligible Instructor correct status. | Refresh or audited attendance update. | Groups tap events into an attendance summary. | Prefer the subject-based workspace for new operation. |
| RFID Attendance Scanner — `/admin/attendance/scanner` | Simple legacy manual scanner screen. | Admin; verified Instructor. | Session status, RFID input, result, recent attendance. | Submit RFID scan. | Creates/updates attendance through legacy scan logic. | A second open log becomes checkout. | Separate from the full Console panel; less complete validation/evidence flow. |
| Online Classes — `/admin/online-classes` | Create and manage scheduled online meetings. | Admin; verified Instructor (own classes only). | Schedules, class details, attachments, face requirement, status, attendance link. | Add/edit/reschedule/cancel/delete, upload attachments, view attendance. | Saves class, audit row, notifications, emails. | Opening page finalizes ended absences; changes notify students. | Closed-year data is read-only; Instructor must own schedule. Online Classes switch mainly hides menu and is not enforced on every direct route. |
| Online Class Logs — `/admin/online-class-logs` | Audit online-class changes. | Admin only. | Actor, role, action, class, section, before/after values, filters. | Filter and CSV export. | View refresh/download. | Module audit rows are immutable. | Route returns 404 when Online Classes switch is off. |
| Activity Logs — `/admin/activity-logs` | Review general system audit events. | Admin only. | Actor, role, route, action, module, outcome, severity, subject, IP/status/time. | Filter by date/year/semester/user/action/module and export CSV. | View refresh/download. | Opening/exporting is itself audited. | Audit failure never blocks the original user operation, so logs are best-effort. |
| System Settings — `/admin/settings` | Configure feature switches, attendance rules, demo RFIDs, face provider use, questions, and emergency sounds. | Admin only. | All settings and face-provider availability; uploaded sound library. | Toggle/save, set thresholds/questions/demo RFIDs, upload/select/delete sound. | Settings update and selected sound/library changes. | Invalid provider combinations are automatically kept off with a warning. | Sound types/10 MB limit; default sound cannot be deleted; Parent letters depend on Parent Portal. |
| Borrowing — `/admin/borrow` | Issue/return inventory items and review borrowers/transactions. | Admin only; borrowing actions also appear on Console panel. | Borrower profiles, RFID lookup, available items, active/history rows, statuses and counts. | Borrow selected quantities, return items, filter. | Borrowing/line/item status changes. | Header status follows outstanding lines; item status follows availability. | Borrowing switch required; quantity and current item state are checked transactionally. |
| Inventory — `/admin/inventory` | Maintain main inventory items. | Admin only. | Barcode, SKU, name, description, status. | Add/edit/soft-delete items. | `inventory_items` changes. | UI redirects to Settings if disabled. | Inventory switch required; borrowing references can restrict deletion. |
| Standalone RFID — `/admin/rfid` (`Rfid.vue`) | Legacy combined student/Instructor RFID list. | Admin only via direct URL. | Owners and current RFID tags. | Assign/update/clear tag. | RFID field changes. | Uniqueness validation applies. | Hidden from menu; normal workflow uses Student/Instructor Management or Registrar. |
| Students Management Prototype — `/admin/students-management` (`StudentsManagement.vue`) | Older static/prototype student page. | Admin only via direct URL. | Client-side sample management UI. | Prototype add/edit interactions. | Does not represent the canonical Student Management workflow. | None reliable. | Retained route should not be used for production operations. |

## Registrar pages

| Page and route | 1. Purpose | 2. Access | 3. Information displayed | 4. User actions | 5. Result | 6. Automatic behavior | 7. Conditions and restrictions |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Registrar Dashboard — `/registrar/dashboard` | Summarize biometric enrollment work. | Registrar only. | Enrollment totals, daily chart, recent logs. | Open enrollment pages. | Navigation only. | Reads Registrar audit log. | No academic or attendance editing. |
| Student Biometric Enrollment — `/registrar/biometric-enrollment` | Enroll student RFID and face images. | Registrar only. | Searchable students, placement/status, RFID, face images and enrollment completeness. | Assign/update RFID, capture/upload image, delete image. | Student identity and Registrar audit log change. | Images store on public disk; completion statistics recalculate. | Unique RFID; image type/size/count rules; page intentionally lists students only. |
| Instructor Faces — `/registrar/instructor-face-enrollment` | Enroll Instructor RFID and face images. | Registrar only. | Searchable Instructor accounts, RFID, face images. | Assign RFID, capture/upload/delete face. | User identity and audit log change. | Images store on public disk. | Only users whose role is Instructor; uniqueness/file limits apply. |

## Console attendance page

| Page and route | 1. Purpose | 2. Access | 3. Information displayed | 4. User actions | 5. Result | 6. Automatic behavior | 7. Conditions and restrictions |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Attendance Control Panel — `/attendance-control-panel` | Operate a laboratory's live RFID attendance, borrowing, and emergency station. | Console role after room/PIN verification. | Room/device/session state, schedule, Instructor/student cards, tap log, camera, borrowing catalog, emergency types/hotlines, demo buttons when enabled. | Start/pause/continue/end attendance, scan Instructor/student RFID, complete face/fallback checks, authorize temporary movement, Dismiss/Continue Class, borrow items, send emergency, log out. | Creates session/attendance/tap/borrowing/alert records and immediate feedback. | Polls status/log snapshots; resolves current schedule; consumes short-lived verification; finalizes cutting/missed movement on session change. | Active device/current-year schedule/enrollment required; feature switches apply; exact rules are in the tapping guide. |

## Student and Parent portal

| Page and route | 1. Purpose | 2. Access | 3. Information displayed | 4. User actions | 5. Result | 6. Automatic behavior | 7. Conditions and restrictions |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Portal Dashboard — `/student-parent/dashboard` | Personal/linked-student overview. | Student; Parent when enabled. | Selected student, attendance counts/recent records, upcoming online classes, notifications/messages/letters summaries. | Parent switches student; open detail pages. | Scoped data refresh/navigation. | Defaults to current/latest enrollment/year. | Student sees self; Parent only linked students. |
| Portal Profile — `/student-parent/profile` | Update account and view student record. | Student; Parent when enabled. | Account contact information and selected student's read-only school record. | Update permitted account fields and password. | User row/password changes. | Password is hashed and actions audited. | Cannot change official student identity/placement. |
| Portal Attendance — `/student-parent/attendance` | Review physical and online attendance. | Student; Parent when enabled. | Year filters, summaries, detailed rows, taps/evidence links. | Select linked student/year, filter and view evidence. | Read-only refresh/file response. | Combines physical and online attendance and derives labels. | Evidence access checks student ownership/link; no status editing. |
| Portal Online Classes — `/student-parent/online-classes` | View and join eligible meetings. | Student; Parent may view for linked student; Student identity performs join. | Class schedule/status, link, attachments, attendance status, face requirement. | Select year/student, open attachment/link, join with face capture when required. | Creates Present/Late attendance or returns prior record. | Opening page finalizes ended missing attendance. | Join only during time window, active year, matching enrollment; current code expects enrollment status `active`, which may conflict with default `enrolled`. |
| Excuse Letters — `/student-parent/excuse-letters` | Submit, approve, and download absence letters. | Student; Parent when enabled. | Letters/status, recipient suggestions, attachments, approval/signature controls. | Submit, choose teachers, upload support, Parent signs/approves, download generated PDF/attachment. | Letter/PDF/messages/emails are created. | Student submission emails linked parents; approval generates and sends signed PDF. | Parent creation/approval requires both Parent switches; pending letters cannot be downloaded; attendance is not changed automatically. |
| Portal Messages — route uses shared `/messages` (`StudentParent/Messages.vue` is retained compatibility UI) | Communicate with staff or linked users. | Student; Parent when enabled. | Conversations and attachments in selected-student context. | Send/read/download. | Private message state changes. | Email cooldown and unread polling apply. | Current navigation opens the shared `Messages/Index.vue`; this specialized file is not directly rendered by current routes. |
| Notifications — `/student-parent/notifications` | Review online-class notifications. | Student; Parent when enabled. | Created/updated/rescheduled/cancelled class notices and read state. | Select linked student and mark read. | `read_at` changes. | Notifications are created during class changes and email is attempted. | Notification must belong to selected/linked student. |

## Clinic and emergency pages

| Page and route | 1. Purpose | 2. Access | 3. Information displayed | 4. User actions | 5. Result | 6. Automatic behavior | 7. Conditions and restrictions |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Clinic Dashboard — `/clinic/dashboard` | Receive and coordinate emergency response. | Clinic; Admin may open route but menu is Clinic-only. | Open alerts, timers/details/students/SMS result, emergency types, calendar, selected sound, personal dispatch assignments. | Enable audio, acknowledge/resolve/cancel, dispatch to Clinic responder, add/edit/delete emergency types. | Alert metrics/status, assigned Clinic Cases, notifications and audits change. | New open alerts trigger selected sound after browser permission; response time calculated. | Dispatch needs active Clinic responder; external emergency contact is not automatic. |
| Case Logs — `/clinic/case-logs` | Maintain treatment/incident cases. | Clinic; Admin via direct route. | Case, patient, alert, handler, symptoms, actions, notes, status/time. | Add/edit case; create Patient History from case. | Case/history records change. | Dispatch-created cases appear automatically. | Validation and role protection apply; patient may be student, user, visitor/general incident. |
| Patient History — `/clinic/patient-history` | Maintain longer-term patient records. | Clinic; Admin via direct route. | Patient type/name, summary, notes, occurrence and recorder. | Add/edit/delete. | History changes. | Audited. | Sensitive role-protected information; linked people may be nullable. |
| Clinic Reports — `/clinic/reports` | Filter clinic-specific operational analytics. | Clinic; Admin. | Alert/case totals, type/status/trend charts, recent cases. | Filter and export CSV. | View refresh/download. | Aggregates live clinic tables; export is audited. | This is separate from shared role-aware Reports. |
| Emergency Hotlines — `/clinic/emergency-hotlines` | Configure alert contact/SMS destinations. | Clinic; Admin via route. | Name, controlled category, phone, contact, SMS/active flags, order, notes. | Add/edit/soft-delete. | Hotline record changes. | Panel selects/routes matching active entries. | A saved hotline does not guarantee SMS delivery; provider credentials/network required. |

## Legacy, reusable, and unwired page files

These source files were reviewed so that their existence is not mistaken for a live feature.

| Page file | Current state | Explanation |
| --- | --- | --- |
| `LandingPage.vue` | Unwired | Older marketing landing page; `/` now renders Student/Parent Login. |
| `Welcome.vue` | Unwired | Starter placeholder. |
| `Classes.vue` | Unwired | Client-side class-management prototype; no backend route. |
| `RFIDRegistration.vue` | Unwired | RFID registration prototype; no current route. |
| `ReusableLandingIndex.vue` | Unwired/reusable | Configurable landing component retained for reuse, but no route is registered. |
| `ReusableAboutPage.vue` | Unwired/reusable | Configurable about component retained for reuse, but no route is registered. |
| `Auth/Login.vue` | Wrapper/legacy | Small wrapper retained from starter auth; custom Staff and Student/Parent pages are the active logins. |
| `Attendance/Dashboard.vue` | Active child view | Rendered by the subject attendance route, not a top-level menu route. |
| `StudentParent/Messages.vue` | Unwired compatibility view | Portal message routes now render the unified `Messages/Index.vue`. |
| `settings/*.vue` | Active wrappers | Thin wrappers around starter-kit settings components; behavior comes from those components/controllers. |

## Buttons, statuses, and messages used across pages

- **Add/Create/Save/Update** validates required fields, stores the record, audits the request, and normally shows a green success flash. Validation failure returns field messages and keeps the form data.
- **Delete** may soft-delete a record or be blocked when history/references exist. Confirmation dialogs do not replace server checks.
- **Cancel** on an online class changes its status and notifies students; **Delete** soft-deletes it. These are different actions.
- **Archive/Close/Reopen/Activate** are academic-year lifecycle actions, not simple labels; they change what can be edited.
- Attendance status labels are Pending, Present, Late, Absent, Excused, and Incomplete Attendance. Tap event labels are Check-in, Temporary Exit, Temporary Return, Check-out, Ignored Tap, Invalid Tap, Manual Edit, and generated No Tap.
- Emergency alert statuses include Open, Acknowledged, Resolved, and Cancelled. Clinic Case status is managed separately.
- Online classes can be Scheduled, Cancelled, or displayed as Completed after their end time.
- Common authorization failures return an access-denied response; state/validation failures return a message explaining why the action cannot proceed.

